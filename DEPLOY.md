# 部署说明

这份说明写给第一次部署网站的人。部署完成后，买家扫二维码进入点单页；摊主用后台处理订单、拣货、核验付款和导出销售数据。

项目不会内置你的真实网址、后台地址、收款码或订单数据。每个人部署后都会拥有自己的网址、自己的数据库和自己的后台密码。

不同平台的页面名称可能会调整，下面的配置以项目需要为准：能运行 Python、能暴露 Web 端口、最好能持久保存数据库文件。

## 先选使用方式

如果只是自己测试：

- 用本地运行。
- 不需要服务器。
- 手机和平板需要和电脑在同一个 Wi-Fi 下。
- 买家离开这个网络后访问不到。

如果要正式办展给买家扫码：

- 部署到公网服务器或云平台。
- 买家用手机流量也能打开。
- 需要特别注意后台路径、后台密码和数据库持久保存。

## 开源前检查

公开仓库前，请确认不要上传这些内容：

- `data/` 文件夹
- `*.sqlite3` 或 `*.db` 数据库文件
- `.env` 环境变量文件
- 真实收款码图片
- 真实订单导出表
- 你自己的后台入口地址

本项目已经在 `.gitignore` 里忽略了常见数据文件，但如果你使用 GitHub 网页手动上传，仍然需要自己确认没有拖错文件。

## 本地运行

适合先在电脑上试用流程。

1. 安装 Python 3.10 或更新版本。
2. 下载或克隆本项目。
3. 进入项目文件夹。
4. 双击 `启动摆摊工具.bat`。
5. 买家页打开：

```text
http://127.0.0.1:8765
```

6. 后台默认打开：

```text
http://127.0.0.1:8765/admin
```

7. 默认后台密码：

```text
123456
```

第一次进入后台后，请立刻修改密码。

如果手机、平板和电脑在同一个 Wi-Fi 下，可以用电脑的局域网 IP 访问。

Windows 查看局域网 IP：

```powershell
ipconfig
```

找到类似 `192.168.x.x` 的 IPv4 地址后，手机或平板打开：

```text
http://192.168.x.x:8765
```

如果你设置了自定义后台路径，后台地址里的 `/admin` 也要换成你自己的后台路径。

## 公网部署需要什么

你需要一个能运行 Python 或 Docker 的平台，例如：

- Render、Railway、Fly.io 等云平台
- 腾讯云、阿里云、华为云等云服务器
- 其他支持 Python Web Service 的平台

这个项目没有外部 Python 依赖，核心启动命令是：

```bash
python app.py
```

程序会读取平台提供的端口环境变量 `PORT`。如果平台没有提供，默认使用 `8765`。

## 重要：数据库必须持久保存

商品、订单、摊位设置、促销规则和后台密码都会保存在数据库里。

默认数据库位置：

```text
data/booth.sqlite3
```

如果部署平台使用临时文件系统（例如 Render），正式使用时必须同时完成下面两项：

1. 给服务添加持久磁盘或持久卷。
2. 用 `BOOTH_DATA_DIR` 把数据库目录指向这块磁盘的挂载路径。

例如，磁盘挂载路径是 `/data` 时，环境变量应当设置为：

```text
BOOTH_DATA_DIR=/data
```

磁盘路径和环境变量必须完全一致。只填写 `BOOTH_DATA_DIR` 并不会自动创建持久磁盘；只添加磁盘但把数据库写到其他目录，也无法保存数据。

> **警告：** 如果没有持久存储，服务重启、休眠或重新部署后，商品、订单、摊位设置、后台密码和收款码都可能一起恢复为默认状态。正式录入数据前，务必完成持久化配置并进行重启测试。

## 推荐环境变量

正式使用时建议至少设置：

```text
ADMIN_PATH=/换成只有你知道的后台路径
```

如果部署平台的项目文件会在重启或重新部署后清空，则必须添加持久磁盘并设置：

```text
BOOTH_DATA_DIR=/data
```

变量说明：

| 变量名 | 是否必需 | 作用 |
| --- | --- | --- |
| `ADMIN_PATH` | 正式使用强烈建议 | 修改后台入口，避免默认 `/admin` 被猜到 |
| `BOOTH_DATA_DIR` | 使用临时文件系统的平台时必需 | 指定数据库保存目录，必须与持久磁盘挂载路径一致 |
| `PORT` | 通常平台自动提供 | 网站监听端口 |
| `HOST` | 通常不用改 | 默认本地运行，Docker 中会设为 `0.0.0.0` |

后台路径示例：

```text
ADMIN_PATH=/my-booth-workroom-2026
```

设置后，后台地址会变成：

```text
https://你的网站地址/my-booth-workroom-2026
```

买家二维码只放首页地址，不要公开后台地址。

## Render 部署（推荐）

Render 可以直接连接 GitHub 部署，但这个工具使用 SQLite 保存商品、密码和订单，因此正式使用时需要：

- 一个付费 Web Service 实例，例如 `Starter`。
- 一块挂载到该服务的 Persistent Disk（持久磁盘）。
- 正确的 `BOOTH_DATA_DIR` 环境变量。

> **不要在 Render Free 实例中录入正式数据。** Free 实例不能挂载持久磁盘，并且会休眠；服务休眠、重启或重新部署后，本地数据库可能消失。Free 只适合临时查看界面和测试流程。

Render 官方说明：

- [Persistent Disks](https://render.com/docs/disks)
- [Free Web Services](https://render.com/docs/free)

### 第一步：创建 Web Service

1. 把项目上传到 GitHub。
2. 打开 Render，新建 Web Service。
3. 连接你的 GitHub 仓库。
4. 选择付费实例，例如 `Starter`。
5. 按下面配置填写：

```text
Language: Python 3
Branch: main
Root Directory: 留空，除非你的代码放在仓库子目录
Build Command: 可留空；如果平台要求填写，可以填 python --version
Start Command: python app.py
```

如果你的仓库根目录不是这个工具，而是把工具放在 `booth_order_tool/` 子目录里，则 `Root Directory` 填：

```text
booth_order_tool
```

### 第二步：添加持久磁盘

在创建服务页面的 `Advanced` 区域添加磁盘，或者部署后进入服务的 `Disks` 页面添加：

```text
Name: booth-data（名称可以自定义）
Mount Path: /var/data
Size: 1 GB
```

1 GB 足够初次使用，后续可以扩容。只有写入 `/var/data` 下面的文件才会被持久保存。

### 第三步：设置环境变量

在服务的 `Environment` 页面添加：

```text
ADMIN_PATH=/换成只有你知道的后台路径
BOOTH_DATA_DIR=/var/data
```

这里的 `/var/data` 必须与磁盘的 `Mount Path` 完全相同。例如磁盘挂载在 `/data`，环境变量也必须改为：

```text
BOOTH_DATA_DIR=/data
```

不要把磁盘挂载在 `/var/data`，却把环境变量写成 `/data`。这会让程序在临时文件系统中创建另一份数据库，看起来就像数据突然重置。

### 第四步：部署并打开网站

完成磁盘和环境变量设置后重新部署。Render 会给你一个网站地址。

买家地址通常是：

```text
https://你的网站地址
```

后台地址是：

```text
https://你的网站地址/你的后台路径
```

每次修改 GitHub 代码后，如果平台没有自动部署，就在 Render 里手动选择部署最新提交。

### 第五步：录入正式数据前进行持久化自检

不要一部署完成就录入全部商品。请先做下面的测试：

1. 进入后台修改默认密码。
2. 添加一个写有“持久化测试”的临时商品。
3. 打开 Render 的 `Shell` 页面，运行：

```bash
echo "$BOOTH_DATA_DIR"
ls -lah "$BOOTH_DATA_DIR"
```

正常情况下，第一条命令应显示磁盘挂载路径，例如 `/var/data`；第二条命令应看到：

```text
booth.sqlite3
```

4. 在 Render 中选择 `Restart service`，等待网站恢复。
5. 再次登录后台，确认新密码和“持久化测试”商品仍然存在。
6. 测试通过后，再删除测试商品并录入正式数据。

### 如果密码和商品突然一起恢复默认

这通常不是程序定时清空，而是程序连接到了一份新创建的空数据库。依次检查：

1. 服务是否仍然是付费实例，而不是 `Free`。
2. `Disks` 页面是否确实存在持久磁盘。
3. 磁盘 `Mount Path` 是否与 `BOOTH_DATA_DIR` 完全一致。
4. Shell 中的目标目录是否存在 `booth.sqlite3`。

如果磁盘仍然存在，只是环境变量路径填错，旧数据库可能还保留在原磁盘中。此时先不要继续录入新商品，应先修正路径并确认旧数据库是否恢复。

## 腾讯云轻量服务器参考

适合正式办展时使用国内访问更稳定的服务器。

推荐思路：

1. 购买轻量应用服务器。
2. 系统选择 Linux。
3. 安装 Python 3.10 或更新版本。
4. 把项目文件上传到服务器。
5. 在项目目录运行：

```bash
python app.py
```

6. 在服务器防火墙或安全组里放行项目端口，默认是 `8765`。
7. 访问：

```text
http://你的服务器公网IP:8765
```

更正式的做法是再配置域名、HTTPS 和后台常驻运行。第一次使用云服务器可以先跑通 IP 访问，再逐步完善。

正式办展前，请确认：

- 服务器重启后工具还能重新启动。
- `data/booth.sqlite3` 不会被删除。
- 后台路径不是默认 `/admin`。
- 后台密码已经改掉。

## Docker 方式

项目内置 `Dockerfile`，适合支持 Docker 的平台或云服务器。

镜像默认设置：

```text
HOST=0.0.0.0
PORT=8765
BOOTH_DATA_DIR=/data
```

请给 `/data` 挂载持久存储，并按需要设置：

```text
ADMIN_PATH=/换成只有你知道的后台路径
```

## 后台入口与密码

默认后台入口是：

```text
/admin
```

开源后，别人能从代码和文档知道默认入口。因此正式使用时强烈建议设置：

```text
ADMIN_PATH=/换成只有你知道的后台路径
```

默认后台密码是：

```text
123456
```

第一次上线后请立刻进入后台修改密码。

## 上线后的检查清单

1. 打开买家首页，确认商品页能正常加载。
2. 打开后台，确认后台路径不是默认 `/admin`。
3. 使用默认密码登录后，立刻修改后台密码。
4. 上传摊位 Logo、欢迎语、微信收款码和支付宝收款码。
5. 添加商品、库存、标签和赠品。
6. 设置摊员名单。
7. 设置满赠、买赠或满减规则。
8. 试下一单，确认取单码、小票、订单看板和拣货流程正常。
9. 试导出订单明细和商品汇总。
10. 用手机流量打开买家页，确认不在同一 Wi-Fi 下也能访问。
11. 把买家首页网址生成二维码，贴在摊位上。

## 忘记后台密码

请查看：

```text
PASSWORD_RESET.md
```

重点是：密码保存在运行网站的数据库里，不保存在 GitHub 代码里。忘记线上密码时，需要在运行网站的服务器或云平台里执行重置脚本。
